import 'package:flutter/material.dart';

class AppColors {
  // The primary green color seen across the app bars and buttons
  static const Color primaryGreen = Color(0xFF4D7227);
  // A slightly lighter green for input fields and specific containers
  static const Color lightGreen = Color(0xFF6B9243);
  // Background gray for text fields or dividers
  static const Color inputBackground = Color(0xFFF5F5F5);
}
